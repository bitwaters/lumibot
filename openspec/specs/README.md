# Living specs

行为需求在此目录。**数值阈值不以本目录或归档 OpenSpec 为准**，一律以 [`config/chains.yaml`](../../config/chains.yaml) 为准，说明见 [`docs/runtime-params.md`](../../docs/runtime-params.md)。

历史提案（含过期的 `-20%` 硬止损、`$50k–$2M` 市值等）在 `openspec/changes/archive/`。
